# Квадратичная функция и её свойства

---

<LessonSection title="Основные свойства квадратичной функции и её графика">

**Квадратичной функцией** называется функция вида:

$$
y = ax^2 + bx + c
$$

**где:**
* **a, b, c** — коэффициенты, причём **a ≠ 0**
* Графиком квадратичной функции является **парабола**

</LessonSection>

---

<LessonSection title="Направление ветвей параболы">

**Направление ветвей параболы зависит от коэффициента a:**

**Если a > 0**
![](https://i.postimg.cc/fbnB2LNL/Вершина_Параболы.png)

* Ветви параболы направлены **вверх**
* Парабола имеет **минимум** в вершине
* Множество значений: $E(f) = [y_в, +\infty)$

**Если a < 0**
![](https://i.postimg.cc/g2RKZW6K/Вершина_Параболы_a___0.png)

* Ветви параболы направлены **вниз**
* Парабола имеет **максимум** в вершине
* Множество значений: $E(f) = (-\infty, y_в]$

</LessonSection>

---

<LessonSection title="Нули функции и дискриминант">

**Нули функции** — точки пересечения графика с осью OX (значения $x$, при которых $y = 0$). Определяются через дискриминант:

$$
D = b^2 - 4ac
$$

**D > 0 — два различных нуля**
![](https://i.postimg.cc/4yqzYL44/Нули_функции.png)

* Парабола пересекает ось OX в двух точках: $x_1, x_2$
* $y = a(x - x_1)(x - x_2)$

**D = 0 — один нуль (касание)**
![](https://i.postimg.cc/K8nnYD6z/Нули_функции_D=0.png)

* Парабола касается оси OX в одной точке: $x_0$
* $y = a(x - x_0)^2$

**D < 0 — нет нулей**
![](https://i.postimg.cc/sxkhjMKh/Нули_функции_D___0.png)

* Парабола не пересекает ось OX
* Функция не раскладывается на линейные множители

</LessonSection>

---

<LessonSection title="Ось симметрии и вершина параболы">

**Ось симметрии** параболы — вертикальная прямая, проходящая через вершину:

![](https://i.postimg.cc/yNL3JQnM/Ось_симметрии.png)

**Формула оси симметрии:**
$$
x_в = -\frac{b}{2a}
$$

**Координаты вершины** параболы:
$$
x_в = -\frac{b}{2a}, \quad y_в = f(x_в) = -\frac{D}{4a}
$$

**Функция в виде через вершину:**
$$
y = a(x - m)^2 + n
$$
где $(m, n)$ — координаты вершины.

</LessonSection>

---

<LessonSection title="Построение графика квадратичной функции">

**Алгоритм построения параболы:**

**1. Найти вершину и ось симметрии**
Вычисляем координаты вершины: $x_в = -\frac{b}{2a}$, $y_в = f(x_в)$

![](https://i.postimg.cc/PxNK3Fqy/Построение_1.png)

**2. Найти нули функции**
Решаем уравнение $ax^2 + bx + c = 0$
Находим точки пересечения с осью OX (если они существуют)

![](https://i.postimg.cc/3r4n26Vk/Построение_2.png)

**3. Найти точку пересечения с OY и симметричные точки**
Точка пересечения с осью OY: $(0, c)$
Используем симметрию параболы для нахождения дополнительных точек

![](https://i.postimg.cc/mgRjfrD5/Построение_3-4.png)

</LessonSection>

---

<LessonSection title="Монотонность квадратичной функции">

![](https://i.postimg.cc/hv1J9Kqw/Монотонность.png)

**При a > 0:**
* **Убывает** на промежутке: $(-\infty, x_в]$
* **Возрастает** на промежутке: $[x_в, +\infty)$

**При a < 0:**
* **Возрастает** на промежутке: $(-\infty, x_в]$
* **Убывает** на промежутке: $[x_в, +\infty)$

</LessonSection>

---

<LessonSection title="Промежутки знакопостоянства квадратичной функции">

**Промежутками знакопостоянства** называются интервалы значений аргумента $x$, на которых функция сохраняет постоянный знак (положительный или отрицательный).

**Знакопостоянство зависит от:**
1. Знака коэффициента **a**
2. Значения дискриминанта **D**
3. Расположения нулей функции (если они существуют)

</LessonSection>

---

<LessonSection title="Знакопостоянство при a > 0">

**Случай 1: a > 0, D > 0**
![](https://i.postimg.cc/rsfVVsTr/Знакопостоянство.png)

* Функция имеет **два различных нуля**: $x_1$ и $x_2$ ($x_1 < x_2$)
* **Положительна** на: $(-\infty, x_1) \cup (x_2, +\infty)$
* **Отрицательна** на: $(x_1, x_2)$
* **Равна нулю** в: $x = x_1$ и $x = x_2$

**Случай 2: a > 0, D = 0**
![](https://i.postimg.cc/HW9jvWqV/Знакопостоянство_a___0.png)

* Функция имеет **один нуль**: $x_0$
* **Положительна** на: $(-\infty, x_0) \cup (x_0, +\infty)$
* **Отрицательных значений нет**
* **Равна нулю** только в: $x = x_0$

**Случай 3: a > 0, D < 0**
![](https://i.postimg.cc/BvPXmT0c/Знакопостоянство_a___0.png)

* Функция **не имеет нулей**
* **Положительна** на: $(-\infty, +\infty)$
* **Отрицательных значений нет**
* Нигде **не равна нулю**

**Общее правило для a > 0:**
При **a > 0** парабола направлена ветвями вверх. Функция принимает положительные значения вне интервала между нулями (если они есть), и отрицательные значения между нулями. Если нулей нет, функция всегда положительна.

</LessonSection>

---

<LessonSection title="Знакопостоянство при a < 0">

**Случай 1: a < 0, D > 0**
![](https://i.postimg.cc/TPjX21qG/изображение.png)

* Функция имеет **два различных нуля**: $x_1$ и $x_2$ ($x_1 < x_2$)
* **Отрицательна** на: $(-\infty, x_1) \cup (x_2, +\infty)$
* **Положительна** на: $(x_1, x_2)$
* **Равна нулю** в: $x = x_1$ и $x = x_2$

**Случай 2: a < 0, D = 0**
![](https://i.postimg.cc/SxMh5Dnj/изображение.png)

* Функция имеет **один нуль**: $x_0$
* **Отрицательна** на: $(-\infty, x_0) \cup (x_0, +\infty)$
* **Положительных значений нет**
* **Равна нулю** только в: $x = x_0$

**Случай 3: a < 0, D < 0**
![](https://i.postimg.cc/4y2GC0vd/изображение.png)

* Функция **не имеет нулей**
* **Отрицательна** на: $(-\infty, +\infty)$
* **Положительных значений нет**
* Нигде **не равна нулю**

**Общее правило для a < 0:**
При **a < 0** парабола направлена ветвями вниз. Функция принимает отрицательные значения вне интервала между нулями (если они есть), и положительные значения между нулями. Если нулей нет, функция всегда отрицательна.

</LessonSection>

---

<LessonSection title="Таблица знакопостоянства квадратичной функции">

**Коэффициент a > 0**

| Дискриминант D | Нули функции | y > 0 | y < 0 |
| :--- | :--- | :--- | :--- |
| D > 0 | Два нуля: $x_1, x_2$ $(x_1 < x_2)$ | $(-\infty, x_1) \cup (x_2, +\infty)$ | $(x_1, x_2)$ |
| D = 0 | Один нуль: $x_0$ | $(-\infty, x_0) \cup (x_0, +\infty)$ | ∅ (нет) |
| D < 0 | Нет нулей | $(-\infty, +\infty)$ | ∅ (нет) |

**Коэффициент a < 0**

| Дискриминант D | Нули функции | y > 0 | y < 0 |
| :--- | :--- | :--- | :--- |
| D > 0 | Два нуля: $x_1, x_2$ $(x_1 < x_2)$ | $(x_1, x_2)$ | $(-\infty, x_1) \cup (x_2, +\infty)$ |
| D = 0 | Один нуль: $x_0$ | ∅ (нет) | $(-\infty, x_0) \cup (x_0, +\infty)$ |
| D < 0 | Нет нулей | ∅ (нет) | $(-\infty, +\infty)$ |

**Запомните:**
1. При **a > 0**: функция положительна "снаружи" нулей и отрицательна "между" нулями
2. При **a < 0**: функция отрицательна "снаружи" нулей и положительна "между" нулями
3. Если нулей нет (D < 0): знак функции совпадает со знаком коэффициента a на всей числовой прямой

</LessonSection>

---

<LessonSection title="Анализ конкретных функций">

### Функция 1: $f(x) = -(x - 1,2)^2 + 25$

**Форма записи:** Вид через координаты вершины: $y = a(x - m)^2 + n$

**Область определения:** $D(f) = \mathbb{R}$  
*Квадратичная функция определена для всех действительных чисел*

**Коэффициент a:** $a = -1 < 0$  
*Ветви параболы направлены **вниз***

**Координаты вершины:** $m = 1,2$, $n = 25$  
**Вершина:** $(1,2; 25)$  
**Ось симметрии:** $x = 1,2$

**Множество значений:** $E(f) = (-\infty, 25]$  
*При $a < 0$ функция имеет максимум в вершине*

**Нули функции:**
$$
\begin{aligned}
&-(x - 1,2)^2 + 25 = 0 \\
&(x - 1,2)^2 = 25 \\
&x - 1,2 = \pm5 \\
&x_1 = 1,2 - 5 = -3,8 \\
&x_2 = 1,2 + 5 = 6,2
\end{aligned}
$$
*$D > 0$ — два различных нуля*

**Промежутки монотонности:**
* **Возрастает:** $(-\infty, 1,2]$
* **Убывает:** $[1,2, +\infty)$

---

### Функция 2: $h(x) = 2(x - 1)(x + 4)$

**Форма записи:** Факторизованный вид: $y = a(x - x_1)(x - x_2)$

**Область определения:** $D(h) = \mathbb{R}$

**Коэффициент a:** $a = 2 > 0$  
*Ветви параболы направлены **вверх***

**Нули функции:** Из факторизованного вида: $x_1 = 1$, $x_2 = -4$  
*$D > 0$ — два различных нуля*

**Координаты вершины:**
$$
\begin{aligned}
&x_в = (1 + (-4)) : 2 = (-3) : 2 = -1,5 \\
&y_в = h(-1,5) = 2 \times (-2,5) \times 2,5 = -12,5
\end{aligned}
$$
**Вершина:** $(-1,5; -12,5)$  
*Находим среднее арифметическое нулей*

**Множество значений:** $E(h) = [-12,5, +\infty)$  
*При $a > 0$ функция имеет минимум в вершине*

**Промежутки монотонности:**
* **Убывает:** $(-\infty, -1,5]$
* **Возрастает:** $[-1,5, +\infty)$

---

### Функция 3: $p(x) = -2x^2 + 4x - 2$

**Форма записи:** Общий вид: $y = ax^2 + bx + c$

**Область определения:** $D(p) = \mathbb{R}$

**Коэффициенты:** $a = -2 < 0$, $b = 4$, $c = -2$  
*Ветви параболы направлены **вниз***

**Дискриминант:**
$$
D = b^2 - 4ac = 4^2 - 4 \times (-2) \times (-2) = 16 - 16 = 0
$$
*$D = 0$ — один нуль (касание оси OX)*

**Нуль функции:**
$$
x_0 = -\frac{b}{2a} = -\frac{4}{2 \times (-2)} = -\frac{4}{-4} = 1
$$
**Нуль:** $x = 1$

**Координаты вершины:**
$$
x_в = 1 \text{ (совпадает с нулём)}, \quad y_в = p(1) = -2 \times 1^2 + 4 \times 1 - 2 = -2 + 4 - 2 = 0
$$
**Вершина:** $(1; 0)$  
**Ось симметрии:** $x = 1$

**Множество значений:** $E(p) = (-\infty, 0]$

**Промежутки монотонности:**
* **Возрастает:** $(-\infty, 1]$
* **Убывает:** $[1, +\infty)$

**Факторизованный вид:** $p(x) = -2(x - 1)^2$  
*При $D = 0$ функция имеет вид $y = a(x - x_0)^2$*

</LessonSection>